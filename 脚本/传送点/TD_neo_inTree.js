function start() {
    var a = 3719;
    if (cm.isQuestActive(a)) {
        cm.forceCompleteQuest(a);
        cm.playerMessage("Quest complete")
    }
    a = 3724;
    if (cm.isQuestActive(a)) {
        cm.forceCompleteQuest(a);
        cm.playerMessage("Quest complete")
    }
    a = 3730;
    if (cm.isQuestActive(a)) {
        cm.forceCompleteQuest(a);
        cm.playerMessage("Quest complete")
    }
    a = 3736;
    if (cm.isQuestActive(a)) {
        cm.forceCompleteQuest(a);
        cm.playerMessage("Quest complete")
    }
    a = 3742;
    if (cm.isQuestActive(a)) {
        cm.forceCompleteQuest(a);
        cm.playerMessage("Quest complete")
    }
    a = 3748;
    if (cm.isQuestActive(a)) {
        cm.forceCompleteQuest(a);
        cm.playerMessage("Quest complete")
    }
};