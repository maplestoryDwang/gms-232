function start() {
    var b = cm.getPortal().getId();
    var a = cm.getPortal().getName();
    if (cm.isQuestActive(33512)) {
        cm.warp(330002611, 2)
    } else {
        cm.warp(330000001, 1)
    }
};