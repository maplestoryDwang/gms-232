function start() {
    if (cm.isQuestFinished(34260)) {
        cm.warp(450006300, 2)
    } else {
        cm.playerMessage(-1, "一扇奇怪的门。牢牢封锁住了。");
        cm.playerMessage(5, "一扇奇怪的门。牢牢封锁住了。")
    }
};