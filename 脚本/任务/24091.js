var status = -1;

function start(c, b, a) {
    cm.sendNext("..!");
    cm.gainExp(5000);
    cm.forceCompleteQuest();
    cm.dispose()
}

function end(c, b, a) {
    cm.sendNext("..!");
    cm.gainExp(5000);
    cm.forceCompleteQuest();
    cm.dispose()
};