var status = -1;

function start(c, b, a) {
    cm.sendNext("Please talk to Joyce.");
    cm.forceCompleteQuest();
    cm.dispose()
}

function end(c, b, a) {
    cm.sendNext("Please talk to Joyce.");
    cm.forceCompleteQuest();
    cm.dispose()
};