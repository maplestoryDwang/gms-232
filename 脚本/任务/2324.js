var status = -1;

function start(c, b, a) {
    cm.gainExp(1600);
    cm.forceCompleteQuest();
    cm.dispose()
}

function end(c, b, a) {
    cm.gainExp(1600);
    cm.forceCompleteQuest();
    cm.dispose()
};