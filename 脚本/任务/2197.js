var status = -1;

function start(c, b, a) {
    cm.sendNext("Oh, you already have monster book.");
    cm.forceCompleteQuest();
    cm.dispose()
}

function end(c, b, a) {
    cm.sendNext("Oh, you already have monster book.");
    cm.forceCompleteQuest();
    cm.dispose()
};