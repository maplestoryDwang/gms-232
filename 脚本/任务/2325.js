var status = -1;

function start(c, b, a) {
    cm.gainExp(6000);
    cm.forceCompleteQuest();
    cm.dispose()
}

function end(c, b, a) {
    cm.gainExp(6000);
    cm.forceCompleteQuest();
    cm.dispose()
};