var status = -1;
var selectionLog = [];
function start() {
  action(1, 0, 0);
}
function action(f, E, e) {
  if (status == 0 && f == 0) {
    cm.dispose();
    return;
  }
  status++;
  selectionLog[status] = e;
  var V = -1;
  if (status <= V++) {
    cm.dispose();
  } else if (status === V++) {
    cm.useItem(2210065, false);
    cm.playerMessage(-1, "干掉外星人，抢走钥匙，再使用传送门！");
    cm.dispose();
  }
}