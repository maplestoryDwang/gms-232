var status = -1;
var selectionLog = [];
function action(f, E, e) {
  cm.resetMap(450002250);
  cm.spawnMobMultipler(8642016, 1, 10, 550, 138);
  cm.dispose();
}
function start() {
  status = -1;
  action(1, 0, 0);
}