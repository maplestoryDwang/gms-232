var status = -1;
function action(f, E, e) {
  status++;
  var V = -1;
  if (status <= V++) {
    cm.dispose();
  } else if (status === V++) {
    cm.dispose();
  }
}
function start() {
  status = -1;
  action(1, 0, 0);
}
;